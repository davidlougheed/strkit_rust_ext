[build-system]
requires = ["maturin>=1.13.1,<1.14"]
build-backend = "maturin"

[project]
name = "strkit_rust_ext"
requires-python = ">=3.10"
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
]
dynamic = ["version"]
